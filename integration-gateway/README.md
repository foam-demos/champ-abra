# Integration Gateway

Manages connections to 200+ external tools (Zendesk, Salesforce, Intercom, etc.) with rate limiting, retry logic, and credential management. Provides a unified interface for agent services to interact with customer systems.

**Tech stack:** TypeScript, Node.js, Express, Redis, PostgreSQL

**Run locally:** `npm install && npm run dev`