# Browser Automation Service

Executes browser-based agent workflows using Playwright with anti-detection and captcha solving. Handles authentication flows, multi-tab coordination, and form submission for legacy enterprise portals.

**Tech stack:** Python 3.11, Playwright, FastAPI, Redis for session management

**Run locally:** `poetry install && poetry run python src/main.py`